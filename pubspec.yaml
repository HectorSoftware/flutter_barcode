name: flutter_barcode
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  #---------------Dependencias de Jesus
  cupertino_icons: ^0.1.2
  barcode_scan: ^1.0.0

  #---------------Dependencias de Cesar
  sqflite: ^1.1.5
  path_provider: ^0.5.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true